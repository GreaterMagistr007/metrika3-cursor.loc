# 🏢 Metrika3 - Система управления кабинетами

<p align="center">
  <img src="https://img.shields.io/badge/Laravel-12.x-red.svg" alt="Laravel Version">
  <img src="https://img.shields.io/badge/Vue.js-3.x-green.svg" alt="Vue.js Version">
  <img src="https://img.shields.io/badge/PHP-8.2+-blue.svg" alt="PHP Version">
  <img src="https://img.shields.io/badge/Tests-Passing-brightgreen.svg" alt="Tests Status">
</p>

## 📋 Описание проекта

Metrika3 - это современная система управления кабинетами с поддержкой Telegram Mini App, построенная на Laravel 12 и Vue.js 3. Система предоставляет полный функционал для управления пользователями, кабинетами, правами доступа и аудитом действий.

## ✨ Основные возможности

- 🔐 **Аутентификация через Telegram** - Вход в систему через Telegram Mini App
- 👥 **Управление пользователями** - Регистрация, приглашение, управление ролями
- 🏢 **Управление кабинетами** - Создание, настройка, передача прав владения
- 🔑 **Система прав и ролей** - Гибкая система разрешений (admin, manager, operator)
- 📊 **Аудит и логирование** - Полное отслеживание действий пользователей
- 🛡️ **Админ-панель** - Веб-интерфейс для администраторов с ограничением по IP
- 💬 **Система сообщений** - Toast уведомления, постоянные сообщения, broadcast
- 📱 **Уведомления** - Центр уведомлений с непрочитанными сообщениями
- 🧪 **Автотестирование** - Полное покрытие тестами всех функций бэкенда

## 🚀 Быстрый старт

### Установка

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd metrika3-cursor.loc
   ```

2. **Установка зависимостей**
   ```bash
   composer install
   npm install
   ```

3. **Настройка окружения**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

4. **Настройка базы данных**
   ```bash
   php artisan migrate
   php artisan db:seed
   ```

5. **Настройка админ-панели**
   ```bash
   # Создание администратора
   php artisan tinker --execute="App\Models\AdminUser::create(['phone' => '+71234567890', 'name' => 'Admin', 'role' => 'admin']);"
   
   # Настройка IP-ограничения в .env
   ADMIN_IP=127.0.0.1  # Замените на ваш IP-адрес
   ```

6. **Запуск сервера**
   ```bash
   php artisan serve
   npm run dev
   ```

## 🧪 Тестирование

### Запуск автотестов

```bash
# Запуск всех тестов
php run_tests.php

# Или через PHPUnit
vendor/bin/phpunit tests/Feature/

# Запуск конкретных тестов
vendor/bin/phpunit tests/Feature/AuthTest.php
vendor/bin/phpunit tests/Feature/CabinetTest.php
vendor/bin/phpunit tests/Feature/CabinetUserTest.php
vendor/bin/phpunit tests/Feature/PermissionTest.php
vendor/bin/phpunit tests/Feature/AuditTest.php
```

## 🤖 Telegram Bot команды

### Проверка сообщений бота

```bash
# Проверить новые сообщения в боте
php artisan telegram:check

# Проверить с определенного offset
php artisan telegram:check --offset=12345

# Добавить в cron для автоматической проверки каждые 5 минут
*/5 * * * * cd /path/to/project && php artisan telegram:check >> /dev/null 2>&1
```

### Утилиты для разработки

```bash
# Получить информацию о Telegram боте
php artisan telegram:info

# Создать тестового пользователя
php artisan test:create-user

# Создать тестового пользователя с кастомными данными
php artisan test:create-user --phone=+79998887766 --name="Test User" --telegram-id=123456789
```

### Настройка Telegram Bot

1. **Создание бота через @BotFather**
   - Отправьте команду `/newbot`
   - Укажите имя и username бота
   - Получите токен бота

2. **Настройка переменных окружения**
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_BOT_SECRET=your_bot_secret_here
   ```

3. **Настройка команды /start**
   - Бот автоматически обрабатывает команду `/start`
   - Создает пользователя в базе данных
   - Отправляет ссылку для входа в приложение

4. **Mini App настройка (для продакшна)**
   - Используйте команду `/newapp` в @BotFather
   - Укажите URL: `https://yourdomain.com/telegram-register`
   - Настройте кнопку "Открыть приложение"

## 🛡️ Админ-панель

### Доступ к админ-панели

Админ-панель доступна по адресу `/admin` и имеет следующие особенности:

- **IP-ограничение**: Доступ разрешен только с IP-адреса, указанного в переменной `ADMIN_IP` в файле `.env`
- **Аутентификация**: Вход по номеру телефона с OTP-кодом
- **Единообразный интерфейс**: Использование компонента PhoneInput для ввода номера телефона
- **Функционал**: Управление пользователями, кабинетами, просмотр логов аудита, управление сообщениями

### Настройка IP-ограничения

1. **Укажите ваш IP-адрес в .env**
   ```env
   ADMIN_IP=192.168.1.100  # Замените на ваш реальный IP
   ```

2. **Для локальной разработки**
   ```env
   ADMIN_IP=127.0.0.1
   ```

3. **Для продакшна**
   ```env
   ADMIN_IP=your.server.ip.address
   ```

### Создание администратора

```bash
# Создание администратора через tinker
php artisan tinker --execute="App\Models\AdminUser::create(['phone' => '+71234567890', 'name' => 'Admin', 'role' => 'admin']);"
```

## 📱 PhoneInput компонент

### Особенности компонента

PhoneInput - это переиспользуемый Vue компонент для ввода номеров телефонов с автоматическим форматированием:

- **Автоматическое форматирование**: Номер форматируется в процессе ввода (+7 (XXX) XXX-XX-XX)
- **Умное позиционирование курсора**: Курсор всегда остается в конце строки при вводе
- **Валидация**: Проверка корректности номера телефона с отображением ошибок
- **Защита от дублирования**: Предотвращение появления нескольких символов "+"
- **Поддержка вставки**: Корректная обработка вставки номеров из буфера обмена
- **Disabled состояние**: Возможность блокировки ввода во время загрузки

### Использование

```vue
<PhoneInput
  v-model="phone"
  :error="phoneError"
  :disabled="loading"
  placeholder="+7 (XXX) XXX-XX-XX"
/>
```

### Где используется

- Страница регистрации пользователей
- Страница входа в систему
- Страница заполнения профиля
- Страница авторизации администратора
- Страница регистрации через Telegram

## 💬 Система сообщений и уведомлений

### Возможности системы

Система сообщений Metrika3 предоставляет полный функционал для отправки и управления уведомлениями:

- **Toast уведомления** - Временные всплывающие уведомления
- **Постоянные сообщения** - Сообщения, которые остаются до прочтения
- **Broadcast сообщения** - Системные объявления для всех пользователей
- **Типизированные сообщения** - Success, Error, Warning, Info, System с цветовым кодированием
- **Центр уведомлений** - Dropdown с непрочитанными сообщениями в хедере
- **Страница сообщений** - Отдельная страница для просмотра всех сообщений
- **Автоматическая отметка о прочтении** - При клике на сообщение оно отмечается как прочитанное

### Типы сообщений

- **Success** (зеленый) - Успешные операции
- **Error** (красный) - Ошибки и проблемы
- **Warning** (желтый) - Предупреждения
- **Info** (синий) - Информационные сообщения
- **System** (фиолетовый) - Системные уведомления

### Получатели сообщений

- **Все пользователи** - Broadcast для всех зарегистрированных пользователей
- **Пользователи кабинета** - Сообщения для всех участников конкретного кабинета
- **Конкретный пользователь** - Персональные сообщения

### Валидация получателей

- **Взаимоисключение**: Если выбрано "Все пользователи", другие получатели не добавляются
- **Обязательные поля**: ID кабинета/пользователя обязательны для соответствующих типов
- **Проверка существования**: Серверная валидация существования кабинетов и пользователей
- **Защита от ошибок БД**: Проверки перед созданием связей в базе данных

### Админ-панель для сообщений

- **Создание сообщений** - Модальное окно с полной формой
- **Редактирование сообщений** - Полное редактирование с сохранением изменений
- **Управление получателями** - Динамическое добавление/удаление получателей
- **Статистика сообщений** - Счетчики по типам и статусам
- **Поиск и фильтрация** - Поиск по тексту/заголовку, фильтрация по типу/статусу
- **Активация/деактивация** - Переключение статуса сообщений

### Покрытие тестами

- ✅ **Аутентификация** - Регистрация, вход, верификация OTP
- ✅ **Кабинеты** - CRUD операции, права доступа
- ✅ **Управление пользователями** - Приглашение, удаление, передача прав
- ✅ **Система прав** - Роли, разрешения, проверка доступа
- ✅ **Аудит** - Логирование действий, отслеживание изменений
- ✅ **Система сообщений** - API эндпоинты, MessageService, middleware
- ✅ **Админ-панель** - Управление пользователями, кабинетами, логами, сообщениями

## 📁 Структура проекта

```
app/
├── Http/
│   ├── Controllers/Api/     # API контроллеры
│   ├── Requests/Api/        # Валидация запросов
│   └── Resources/           # API ресурсы
├── Models/                  # Eloquent модели
├── Services/                # Бизнес-логика
└── Traits/                  # Переиспользуемые трейты

tests/
├── Feature/                 # Функциональные тесты
└── Unit/                    # Модульные тесты

database/
├── factories/               # Фабрики для тестов
├── migrations/              # Миграции БД
└── seeders/                 # Сидеры БД
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/verify-otp` - Верификация OTP
- `GET /api/auth/me` - Получение профиля
- `POST /api/auth/logout` - Выход из системы

### Кабинеты
- `GET /api/cabinets` - Список кабинетов
- `POST /api/cabinets` - Создание кабинета
- `GET /api/cabinets/{id}` - Детали кабинета
- `PUT /api/cabinets/{id}` - Обновление кабинета
- `DELETE /api/cabinets/{id}` - Удаление кабинета

### Управление пользователями
- `POST /api/cabinets/{id}/invite` - Приглашение пользователя
- `DELETE /api/cabinets/{id}/users/{user}` - Удаление пользователя
- `PATCH /api/cabinets/{id}/transfer-ownership` - Передача прав владения

### Сообщения
- `GET /api/messages` - Получение всех сообщений пользователя
- `GET /api/messages/unread` - Получение непрочитанных сообщений
- `POST /api/messages/{id}/read` - Отметка сообщения как прочитанного
- `POST /api/messages/mark-all-read` - Отметка всех сообщений как прочитанных

### Админ-панель (только с разрешенного IP)
- `POST /api/admin/auth/login` - Вход администратора
- `POST /api/admin/auth/logout` - Выход администратора
- `GET /api/admin/auth/profile` - Профиль администратора
- `GET /api/admin/statistics` - Общая статистика
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/cabinets` - Список кабинетов
- `GET /api/admin/audit-logs` - Логи аудита
- `GET /api/admin/messages` - Управление сообщениями
- `POST /api/admin/messages` - Создание сообщения
- `PUT /api/admin/messages/{id}` - Редактирование сообщения
- `DELETE /api/admin/messages/{id}` - Удаление сообщения
- `PATCH /api/admin/messages/{id}/toggle-active` - Переключение активности

## 🛠️ Технологии

- **Backend**: Laravel 12, PHP 8.2+
- **Frontend**: Vue.js 3, Vite, TailwindCSS
- **База данных**: SQLite (разработка), MySQL/PostgreSQL (продакшн)
- **Тестирование**: PHPUnit
- **Аутентификация**: Laravel Sanctum
- **Интеграция**: Telegram Bot API
- **Сообщения**: Toast уведомления, постоянные сообщения, broadcast
- **UI компоненты**: Переиспользуемые Vue компоненты (PhoneInput, SelectInput, Toast)

## 🧪 Проверка системы сообщений

### Проверка в клиентской части

1. **Центр уведомлений в хедере**
   - Откройте приложение и авторизуйтесь
   - В хедере должна быть иконка сообщений (чаты с тремя точками)
   - При клике открывается dropdown с непрочитанными сообщениями
   - Кнопка "Показать все уведомления" всегда видна

2. **Страница сообщений**
   - Перейдите на `/messages` или нажмите "Показать все уведомления"
   - Должны отображаться все сообщения с типами и цветовыми бейджами
   - При клике на сообщение оно отмечается как прочитанное
   - Счетчик непрочитанных обновляется в реальном времени

3. **Toast уведомления**
   - При выполнении операций появляются всплывающие уведомления
   - Уведомления имеют минимальную ширину 420px
   - Автоматическое скрытие через 5 секунд

### Проверка в админ-панели

1. **Управление сообщениями**
   - Войдите в админ-панель (`/admin`)
   - Перейдите в раздел "Сообщения"
   - Счетчики статистики должны показывать корректные данные

2. **Создание сообщения**
   - Нажмите "Создать сообщение"
   - Заполните форму с получателями
   - Валидация должна работать для всех полей
   - Сообщение должно создаться и появиться в списке

3. **Редактирование сообщения**
   - Нажмите "Редактировать" у любого сообщения
   - Модальное окно должно открыться с заполненными данными
   - Изменения должны сохраняться корректно

4. **Поиск и фильтрация**
   - Поиск должен работать по заголовку и тексту
   - Фильтры по типу и статусу должны работать
   - Поиск должен поддерживать кириллицу

## 📝 Требования к разработке

- **ВСЕ** функции бэкенда должны быть покрыты тестами
- После каждого шага разработки запускать полный набор тестов
- Все тесты должны проходить успешно перед переходом к следующему шагу
- При добавлении новой функциональности создавать соответствующие тесты

## 📄 Лицензия

Проект разрабатывается в рамках внутреннего использования.
